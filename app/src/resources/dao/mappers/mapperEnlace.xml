<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pit">
  	
	<select id="SQL_ListarEnlacesDePerfil" resultType="EnlaceDTOIbatis" parameterType="int">
		select 
			e.enlace_id as enlaceId,
			e.enlace,
			e.ruta_url as rutaURL,
			e.icono,
			e.enlace_parent_id as enlaceParentId
		from enlace e 
		inner join perfil_enlaces pe on pe.enlace_id = e.enlace_id
		inner join perfil p on p.perfil_id = pe.perfil_id
		where p.perfil_id = #{perfilId};
	</select>
</mapper>